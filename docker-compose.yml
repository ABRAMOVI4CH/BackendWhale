name: whalescale
services:
  api:
    build: .
    ports:
      - "${PORT}:3000"
    environment:
      PORT: "3000"
      DB_PATH: "/app/data/whalescale.sqlite"
      JWT_SECRET: "dev_jwt_secret_change_me"
    volumes:
      - ./data:/app/data

